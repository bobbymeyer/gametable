<!DOCTYPE html>
<html class="dark">
  <head>
    <script>
      (function() {
        var stored = localStorage.getItem("theme");
        document.documentElement.className = stored === "light" ? "light" : "dark";
      })();
    </script>
    <title><%= content_for(:title) || default_page_title %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  </head>

  <body class="min-h-screen bg-stone-50 text-stone-900 dark:bg-stone-950 dark:text-stone-200 font-sans text-left">
    <nav class="border-b border-stone-200 bg-white dark:border-stone-800 dark:bg-stone-950 sticky top-0 z-10">
      <div class="mx-auto flex h-14 max-w-7xl items-center justify-between px-4 sm:px-6">
        <%= link_to root_path, class: "font-semibold text-stone-900 dark:text-stone-100 hover:text-amber-500 dark:hover:text-amber-400 transition-colors" do %>
          <%= app_name %>
        <% end %>
        <div class="flex items-center gap-4">
          <button type="button" id="theme-toggle" aria-label="Toggle light/dark mode" class="p-2 text-stone-500 hover:text-stone-700 hover:bg-stone-200 dark:text-stone-400 dark:hover:text-stone-200 dark:hover:bg-stone-800 transition-colors">
            <span class="theme-icon-dark hidden dark:inline" aria-hidden="true"><svg class="size-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/></svg></span>
            <span class="theme-icon-light inline dark:hidden" aria-hidden="true"><svg class="size-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/></svg></span>
          </button>
          <% if signed_in? %>
            <span class="text-sm text-stone-600 dark:text-stone-400"><%= current_user.email %></span>
            <%= button_to "Sign out", logout_path, method: :delete, class: "border border-stone-400 dark:border-stone-600 px-3 py-1.5 text-sm font-medium text-stone-700 dark:text-stone-300 hover:border-stone-500 hover:bg-stone-200 dark:hover:border-stone-500 dark:hover:bg-stone-800/50 transition-colors" %>
          <% else %>
            <%= link_to "Sign in", login_path, class: "text-sm font-medium text-stone-600 dark:text-stone-400 hover:text-stone-900 dark:hover:text-stone-200 transition-colors" %>
            <%= link_to "Sign up", new_user_path, class: "bg-amber-500 px-3 py-1.5 text-sm font-semibold text-stone-950 hover:bg-amber-400 transition-colors" %>
          <% end %>
        </div>
      </div>
    </nav>

    <%= render "layouts/flash" if flash.any? %>

    <main class="mx-auto max-w-7xl px-4 sm:px-6">
      <%= yield %>
    </main>

    <script>
      (function() {
        function showFlashNotification(message) {
          var wrapper = document.createElement("div");
          wrapper.className = "flash fixed left-0 right-0 top-0 z-50 m-8 mx-auto max-w-[80%] rounded-lg border border-stone-300 dark:border-stone-600 shadow-md overflow-hidden transition-opacity duration-300 ease-out";
          wrapper.setAttribute("aria-atomic", "true");
          var inner = document.createElement("div");
          inner.setAttribute("role", "status");
          inner.setAttribute("aria-live", "polite");
          inner.className = "w-full px-4 py-3 text-sm text-left text-amber-900 dark:text-amber-200 bg-amber-200/80 dark:bg-amber-500/20";
          inner.textContent = message;
          wrapper.appendChild(inner);
          document.body.appendChild(wrapper);
          var timeoutId;
          function scheduleDismiss() {
            clearTimeout(timeoutId);
            timeoutId = setTimeout(dismiss, 3000);
          }
          function dismiss() {
            wrapper.classList.add("opacity-0");
            wrapper.addEventListener("transitionend", function remove() {
              wrapper.remove();
              wrapper.removeEventListener("transitionend", remove);
            });
          }
          wrapper.addEventListener("mouseenter", function() {
            clearTimeout(timeoutId);
          });
          wrapper.addEventListener("mouseleave", function() {
            scheduleDismiss();
          });
          scheduleDismiss();
        }
        var toggle = document.getElementById("theme-toggle");
        if (!toggle) return;
        toggle.addEventListener("click", function() {
          var isDark = document.documentElement.classList.contains("dark");
          var next = isDark ? "light" : "dark";
          localStorage.setItem("theme", next);
          document.documentElement.className = next;
          showFlashNotification(next === "light" ? "Light mode" : "Dark mode");
        });
      })();
    </script>
    <script>
      (function() {
        var flash = document.getElementById("flash-container");
        if (!flash) return;
        var timeoutId;
        function scheduleDismiss() {
          clearTimeout(timeoutId);
          timeoutId = setTimeout(dismiss, 3000);
        }
        function dismiss() {
          flash.classList.add("opacity-0");
          flash.addEventListener("transitionend", function remove() {
            flash.remove();
            flash.removeEventListener("transitionend", remove);
          });
        }
        flash.addEventListener("mouseenter", function() {
          clearTimeout(timeoutId);
        });
        flash.addEventListener("mouseleave", function() {
          scheduleDismiss();
        });
        scheduleDismiss();
      })();
    </script>
  </body>
</html>
